import os
import sys


if __name__ == "__main__":
    for run in os.listdir("runs"):
        # calculate the ATE, XTE. 
    # Output the ATE, XTE for different runs
    
    pass


# Monte carlo over different runs and calculate the XTE, ATE, load in calibration profile. 